# Kind cluster creation

- name: Create kind cluster
  shell: "kind create cluster --name {{ k8s_build_namespace }}"
  ignore_errors: yes

- name: Cluster creation has been finished
  debug:
    msg: "Cluster creation of kind cluster {{ k8s_build_namespace }} has been finished."